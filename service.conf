# Factory service manifest (sourced by bin/network-app; see docs/adding-apps.md)
SERVICE_PORT=8092
SERVICE_RUN="./factory serve"
SERVICE_BUILD="bun install --cwd frontend --frozen-lockfile && bun run --cwd frontend build && go build -trimpath -ldflags='-s -w' -o factory ."
SERVICE_HEALTHZ="/api/healthz"
