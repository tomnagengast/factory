P1-1: The plan is based on an obsolete branch and omits runtime behavior now required by `main`. Plan lines 22, 32-38, and 42-50 modify four files that have also changed upstream. Command evidence shows `HEAD=f18c34b`, merge base `606360a`, and `git rev-list --left-right --count HEAD...origin/main` returns `3 14`; `internal/eventwire/journal.go`, `internal/eventwire/wire.go`, `internal/activity/store.go`, and `internal/server/server.go` are all modified in those 14 upstream commits. The mismatch is substantive: `origin/main:internal/activity/store.go:139-150` adds `StagedPayload`, which `origin/main:internal/server/server.go:676-692` requires for project onboarding, while plan line 34’s retained-method list omits it and line 36 proposes a replacement lookup. Upstream also adds durable rejection/status behavior at `origin/main:internal/eventwire/journal.go:29-46,256-297,330-341` and permanent-failure isolation at `origin/main:internal/eventwire/wire.go:97-138`. Implementing the plan as written risks compile conflicts or regression of project onboarding and wire failure isolation during integration. Smallest correction: add an initial synchronization with current `origin/main`, revalidate the plan against the merged source, and explicitly preserve/reuse `StagedPayload`, project-setup dispatch, `Wire.Status`, and permanent rejection behavior.

P2-1: The route strategy needs one implementation clarification. Plan lines 47, 50, and 104 require malformed paths to return `404`, but the current router starts with `http.NewServeMux` at `internal/server/server.go:264`. `go doc net/http.ServeMux` confirms that dot segments and repeated slashes are redirected before handler matching. Explicit SPA routes plus the static fallback therefore cannot alone guarantee the required no-redirect behavior. Specify a pre-mux canonical-path guard and semantic validation for the issue/start page route.

P2-2: Filter-count semantics are ambiguous. Plan lines 25 and 58 use API counts to populate source/type controls but do not state whether counts are computed before or after active filters. The existing implementation computes counts over the complete retained set before paging (`internal/activity/store.go:203-231`). Clarify full-window or cross-filter facet semantics so selecting one filter does not remove valid alternatives.

VERDICT: REVISE
P1-1
