schema = 1
app = "factory"

[build]
command = """export MISE_BUN_VERSION=1.3.11; bun install --cwd frontend --frozen-lockfile && bun run --cwd frontend build && go build -trimpath -ldflags="-s -w" -o factory ."""

[run]
command = "./factory"

[health]
path = "/api/health"
identity = true
